<h1>Posts</h1>
<%= button_to "sign out", destroy_user_session_path, method: :delete %>
<ul>
  <% @posts.each do |post| %>
      <li>
        <%= post.title %>
        <%= post.body %>
        <% if user_signed_in? %>
          <%= post.user.email %>
        <% end %>
      </li>
  <% end %>
</ul>